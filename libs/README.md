# Anima 依赖库

将以下 JAR 文件放置在此目录：

- `miraclebridge-0.1.0-alpha.jar` - Miracle Bridge 编译依赖

## 获取方式

从 Miracle-Bridge 项目构建：

```bash
cd ../Miracle-Bridge
./gradlew build
cp build/libs/miraclebridge-0.1.0-alpha.jar ../Anima/libs/
```
